This library holds C++ data objects for serde on Choreo's document format

It depends on WPIMath for units library integration and on WPIUtil for JSON serialization.
If built with WITH_TRAJOPT defined, it also depends on TrajoptLib so that it can have conversion functions to/from TrajoptLib's types.
